//creating array of my type->tup[le
const tuple = [23, `soumik`, false];
;
function usecased(key) {
    if (key === 1 /* up */) {
        console.log("up button is pressed");
    }
    else if (key === 0 /* down */) {
        console.log("down key is pressed");
    }
}
//interface
const user = [{ firstname: 'soumik', lastname: 'dey', marks: 100 }];
//optional field comes last in the function arguement
usecased(1);
//# sourceMappingURL=tuples.js.map